[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "graphify"
version = "0.1.0"
description = "Graphify: AST/odvisnostni graf kode za Rob AI Studio."
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools]
packages = ["graphify"]
