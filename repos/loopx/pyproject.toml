[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loopx"
version = "0.1.0"
description = "LoopX: verifikacijska samoozdravitvena zanka za Rob AI Studio."
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools]
packages = ["loopx"]
