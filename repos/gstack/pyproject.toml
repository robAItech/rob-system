[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gstack"
version = "0.1.0"
description = "GStack: arhitekturni manifest in specifikacija modulov za Rob AI Studio."
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools]
packages = ["gstack"]
